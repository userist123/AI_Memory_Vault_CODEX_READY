# Phase 18 — historical calibration projection

Phase 18 is a research-only adapter between the accepted Phase 17 historical evaluation runner and the existing deterministic calibration scorer.

The adapter validates the complete Phase 17 run before projection. It does not learn calibration parameters, alter the prediction probabilities, or infer historical knowledge timing.
