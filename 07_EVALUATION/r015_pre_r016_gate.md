# R015 pre-R016 gate

This gate measures corpus reconciliation and graph census before production benchmark wiring.

The measurements are generated by `r015_pre_r016_census.py`. No note content or frozen benchmark cases are mutated.
