# Graph expansion budget — measurement

Generated by `30_SCRIPTS/evaluation/run_graph_budget_arms.py` from
`graph_budget_results.json`. No figure in this file is hand-written.

- Benchmark: `07_EVALUATION/heldout_retrieval_benchmark_v2/heldout.json` (frozen by SHA-256, not modified), 32 cases.
- Corpus notes: 926.
- Ranking arm: RANKING_ARM_BASELINE (pinned, as in run_production_arms.build).
- Graph arms run **non-strict**: cases where expansion adds nothing are counted, not raised.

## Read this first: the sample is small

The benchmark has 32 cases. A difference of 1–2 cases is noise, in either direction. Nothing below
is a recommendation. It is the data; the default budget is the owner's decision.

## Arms

`budget` is the number of NEW nodes a query may gain from expansion. `default` is the shipped `None`,
which resolves to `max(0, min(2*seeds, 20) - seeds)`.

| arm | budget | candidate recall | context recall | queries with expansion | mean new nodes / query | mean new nodes / expanded query |
|---|---|---:|---:|---:|---:|---:|
| graph_off | — | 20/29 | 2/29 | 0/32 | 0.00 | — |
| budget_default | default | 20/29 | 2/29 | 0/32 | 0.00 | — |
| budget_5 | 5 | 22/29 | 4/29 | 25/32 | 3.91 | 5.00 |
| budget_10 | 10 | 22/29 | 4/29 | 25/32 | 7.81 | 10.00 |
| budget_20 | 20 | 22/29 | 4/29 | 25/32 | 15.62 | 20.00 |

Recall fractions are over measurable cases only (abstention cases are `UNMEASURABLE`, as in R016).

### Paired against `graph_off`

Cases where the arm gained / lost the metric relative to the reference arm.

| arm | candidate recall gained | candidate recall lost | context recall gained | context recall lost |
|---|---:|---:|---:|---:|
| budget_default | 0 | 0 | 0 | 0 |
| budget_5 | 2 | 0 | 2 | 0 |
| budget_10 | 2 | 0 | 2 | 0 |
| budget_20 | 2 | 0 | 2 | 0 |

## Default budget (`None`): where expansion is impossible

Lexical results entering expansion (seed notes) across the 32 queries: min 0, median 200.0, max 200.

- Queries with 20 or more seeds (default budget is 0, expansion impossible): **25/32**.
- Queries with fewer than 20 seeds (expansion possible): 7/32.
- Queries where the largest explicit budget (`budget_20`) still added no node (nothing to expand from their seeds): 7/32.

### Negative control

At budget `None` and 20+ seeds the number of nodes added must be exactly 0.

- Nodes added on those queries: **0**.
- Nodes added on the remaining queries: 0.
- Control: **PASS** — the measurement sees the phenomenon documented in `memory/controller.py`.

## Errors

None. Every arm ran every case.
