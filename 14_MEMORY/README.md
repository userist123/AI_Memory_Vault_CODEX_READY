# 14 — Memory

Purpose: canonical memory models, lifecycle contracts, schemas and memory knowledge.
Allowed: memory data-model definitions and curated memory artifacts.
Forbidden: raw imports, secrets and executable runtime modules.
