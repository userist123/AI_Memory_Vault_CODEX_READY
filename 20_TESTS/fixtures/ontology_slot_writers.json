{
  "purpose": "Reviewed dispositions of every Python module the static scan finds in a write context for 01_ARCHITECTURE/ontology/slots/. test_ontology_slot_writers.py fails on a module the scan finds that is not listed here, and on a listed module the scan no longer finds.",
  "writers": [
    {
      "path": "30_SCRIPTS/ingestion/apply_row_disposition.py",
      "role": "writer",
      "gate": "disposition manifest (`load_manifest`, `validate_against_slots`, row-coverage checks); dry run unless `--apply`",
      "conclusion": "Gated. Needs a manifest that matches the rows on disk bit for bit."
    },
    {
      "path": "30_SCRIPTS/ingestion/merge_candidate_concepts.py",
      "role": "writer",
      "gate": "verdict manifest (`UngatedCanonicalWrite`, PR #163)",
      "conclusion": "Gated. Refuses a write into the canonical slot files without a manifest; other directories are dry runs."
    },
    {
      "path": "30_SCRIPTS/ingestion/promote_candidate_concept.py",
      "role": "writer",
      "gate": "verdict manifest admitting the concept (`UngatedCanonicalWrite`, added in this change)",
      "conclusion": "Was ungated: flipped any `proposed` row to `promoted` with no manifest. Gated here, with tests and a control."
    },
    {
      "path": "30_SCRIPTS/ingestion/purge_rejected_rows.py",
      "role": "writer",
      "gate": "disposition manifest (`UngatedCanonicalWrite`): `--apply` on the canonical slots needs targets that carry a manifest-supplied disposition; dry run and copies stay free",
      "conclusion": "Gated. Was OPEN: `--apply` deleted rows with no manifest, and `--disposition-manifest ''` switched the only protection off. Gate added in this change, shared with the merge and promotion gates."
    },
    {
      "path": "20_TESTS/ontology/test_row_disposition_applied.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: reads the real slots and copies them into `tmp_path` before any change."
    },
    {
      "path": "20_TESTS/test_book_ingestion_pipeline.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: writes only into `tmp_path` copies; the one call with the canonical directory expects a ValueError before any write."
    },
    {
      "path": "20_TESTS/test_canonical_write_requires_verdicts.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: the merge gate's own tests; the canonical directory is patched to a temporary one."
    },
    {
      "path": "20_TESTS/test_controller_slot_path.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: copies the slots into a temporary vault before calling the controller; the real slots are never touched (a control test checks it)."
    },
    {
      "path": "20_TESTS/test_promote_candidate_concept_gate.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: the promotion gate's tests; the canonical directory is patched to a temporary one."
    },
    {
      "path": "20_TESTS/test_promoter_emits_graph_relations.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: promotes into a `tmp_path` slots directory."
    },
    {
      "path": "20_TESTS/test_purge_canonical_gate.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: the purge gate's tests; the canonical directory is patched to a temporary one."
    },
    {
      "path": "20_TESTS/test_purge_rejected_rows.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: reads the real slots and copies them into `tmp_path` before any change."
    },
    {
      "path": "20_TESTS/test_curriculum_provenance.py",
      "role": "test",
      "gate": "n/a (temporary directories)",
      "conclusion": "Reviewed: curriculum provenance gate tests; writes only into tmp_path slots directory."
    }
  ],
  "not_visible_to_the_scan": [
    {
      "path": "`MemoryController.archive` / `supersede` -> `FileStorageEngine.set()` on the 16 `slot-*` notes",
      "gate": "Authorizer and lifecycle policy only (archive: HUMAN, ADMIN; supersede: HUMAN, ADMIN, AI_AGENT); no verdict or disposition manifest",
      "conclusion": "OPEN, not changed here. Corrects the earlier wording: `update`/`propose` on the slot notes are refused, but only because their `id` is not a uuid, not by policy; `archive` and `supersede` succeed and rewrite the slot file's `lifecycle` (ARCHIVED/SUPERSEDED). Trace, tests and closing options: 07_EVALUATION/ontology_write_paths/CONTROLLER_SLOT_PATH.md."
    }
  ]
}