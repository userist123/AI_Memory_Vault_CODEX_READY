{
  "schema": "exempt-area-secret-allowlist.v1",
  "note": "Each entry was opened and read before listing. An entry means a person judged this match benign, not that the rule is wrong.",
  "entries": [
    {
      "path": ".agents/challenger_m3_2/report.md",
      "rules": [
        "private_key"
      ],
      "reason": "An audit report that quotes the pattern it searched for; the header appears inside a sentence about RSA keys, with no key after it."
    },
    {
      "path": ".agents/skills/rclone-cli/references/providers/sftp.md",
      "rules": [
        "private_key"
      ],
      "reason": "Documents how rclone's key_pem option is formatted, with a placeholder body. Removing the header would make the documentation wrong."
    },
    {
      "path": ".agents/skills/security-review/references/secret-patterns.md",
      "rules": [
        "aws_access_key"
      ],
      "reason": "The key AWS itself publishes in its documentation as an example; it authenticates nothing."
    },
    {
      "path": ".agents/skills/security-review/references/vuln-categories.md",
      "rules": [
        "private_key"
      ],
      "reason": "A security skill listing what a hardcoded secret looks like. Its job is to carry these patterns."
    },
    {
      "path": "02_PRODUCT/projects/workspaces/jarvis_cognitive_brain/tests/unit/test_challenger_m3_2_workers.py",
      "rules": [
        "private_key"
      ],
      "reason": "Test fixture feeding the redaction code a fake key so it can prove it redacts one."
    },
    {
      "path": "20_TESTS/test_exempt_area_secret_scan.py",
      "rules": [
        "aws_access_key",
        "private_key",
        "slack_token",
        "github_token",
        "google_api_key",
        "stripe_live_key",
        "openai_key",
        "anthropic_key"
      ],
      "reason": "The scanner's own tests: each rule is exercised against a synthetic string shaped like the secret it looks for. Without these strings the rules would be untested."
    },
    {
      "path": "20_TESTS/test_model_agent_execution_harness.py",
      "rules": [
        "openai_key"
      ],
      "reason": "A fixture string shaped like an OpenAI key, used to assert that secrets never reach a trace."
    }
  ]
}
