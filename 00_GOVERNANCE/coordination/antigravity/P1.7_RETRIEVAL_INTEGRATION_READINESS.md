# P1.7 Retrieval Evaluation, Corpus Quality & Integration Readiness Report

**Author**: Antigravity  
**Branch**: `antigravity/p1-retrieval-foundation`  
**Audit Baseline Commit**: `2a500b027e0cf06a18db6a0c5cbf34366e5fde51`  
**Date**: 2026-09-05  
**Final Status**:  
- `RETRIEVAL EVALUATED`  
- `CORPUS MEASURED`  
- `GRAPH STATUS KNOWN`  
- `INTEGRATION CONTRACT DOCUMENTED`  
- `TESTS REPORTED`  
- `BLOCKERS EXPLICIT`  
- `PRODUCTION WIRING: NOT DONE`  

---

## 1. Audit Starting Baseline

Execution state verified at the start of this evaluation:
```bash
$ git rev-parse HEAD
2a500b027e0cf06a18db6a0c5cbf34366e5fde51

$ git status --short
(clean)

$ git branch --show-current
antigravity/p1-retrieval-foundation
```

All metrics, corpus measurements, and test proofs in this document are strictly bound to this SHA baseline without mixing historical claims.

---

## 2. Audit of Retrieval Facade & Boundary

The retrieval facade (`cognitive_core/retrieval_facade.py`), boundary adapter (`cognitive_core/retrieval_boundary.py`), and test suites were audited against 13 operational criteria:

1. **Principal Propagation**: Principal identity (`human`, `ai_agent`, `admin`) is mandatory and flows unmodified into the response and audit trace. Missing, empty, or unverified principals fail closed with `PrincipalValidationError`.
2. **ACTIVE + Verified Ceiling**: Strictly capped at `ACTIVE` lifecycle and `verified` verification. Any caller attempts to pass `REVIEW`, `RAW`, `ARCHIVED`, `SUPERSEDED`, or `unverified` raise `BoundaryViolationError`.
3. **Lifecycle Filtering**: Callers may narrow within `{ACTIVE}`; broadening is strictly prevented.
4. **Narrowing vs Broadening**: Caller narrowing by note type (e.g. `types=["knowledge"]`) or lifecycle is honored. Expanding beyond the boundary is blocked fail-closed.
5. **Deterministic Ordering**: Absolute tie-breaking is enforced: `key=lambda h: (-h.score, h.note.id)`. Identical queries yield byte-for-byte identical result rankings and scores across invocations.
6. **Pagination Sequence & Depth**: Identified and resolved a fetch cutoff gap. Retrieval depth is calculated as `fetch_k = max(offset + page_size + 1, 10)`, guaranteeing lookahead to accurately determine `next_page_token` presence across arbitrary page counts without truncating results.
7. **Invalid Pagination Token Handling**: Non-string tokens, non-numeric offsets (`offset:abc`), negative offsets (`offset:-5`), and malformed formats raise `FilterValidationError`, preventing infinite pagination loops.
8. **Boolean `page_size` Guard**: Explicitly rejects boolean values (`page_size=True` or `page_size=False`) with `FilterValidationError`.
9. **Read-Only Behavior**: Filesystem stat snapshots before and after queries confirm 0 file creations, deletions, or modifications.
10. **Trace & Evidence Completeness**: The response trace records `caller_principal`, `request_id`, `boundary_filters`, candidate rankings across arms, and pagination metrics (`page_size`, `offset`, `total_hits`, `returned_count`, `has_next_page`).
11. **Empty Result Behavior**: Queries returning 0 matches produce `results=[]`, `total_hits=0`, `next_page_token=None` gracefully without crashing.
12. **Invalid Filter Handling**: Empty filter sequences, unknown lifecycle tokens, and non-string queries fail closed with specific exception types.
13. **Retriever Contract Isolation**: Underlying retriever is never called if structural or boundary validation fails.

---

## 3. Empirical Retrieval Benchmark

Evaluated via [`cognitive_core/benchmarks/retrieval_evaluation_p17.py`](file:///c:/Users/Marius/Documents/Codex/AI_Memory_Vault_P1/cognitive_core/benchmarks/retrieval_evaluation_p17.py) and preserved in [`07_EVALUATION/ci_evidence/retrieval_p17_evaluation_report.json`](file:///c:/Users/Marius/Documents/Codex/AI_Memory_Vault_P1/07_EVALUATION/ci_evidence/retrieval_p17_evaluation_report.json):

### 3.1 Known-Item Retrieval (ProductionRetrievalFacade)
- **Sampled Notes**: 50 canonical notes
- **Total Queries**: 100 queries (50 title queries + 50 distinctive body snippet queries)
- **Recall@1**: **0.4600** (46.00%)
- **Recall@5**: **0.7900** (79.00%)
- **Recall@10**: **0.9100** (91.00%)
- **MRR**: **0.5995**
- **Misses**: 9
- **Median Latency**: **0.960 ms** (< 1 ms)
- **P95 Latency**: **1.134 ms**
- **Repeatability**: **100% deterministic**

### 3.2 Paraphrase Retrieval (ProductionRetrievalFacade + Local Ollama `qwen2.5-coder:3b`)
- **Queries Requested**: 15 semantic reformulations avoiding source note vocabulary
- **Queries Executed**: 15
- **Recall@1**: **0.3333** (33.33%)
- **Recall@5**: **0.6667** (66.67%)
- **Recall@10**: **0.6667** (66.67%)
- **MRR**: **0.4889**
- **Median Latency**: **1.690 ms**

### 3.3 Multi-Hop & Structural Graph Retrieval
- **Total Probed Pairs**: 600 genuine topological pairs
- **Direct Lexical Hits (BM25 + Entity)**: 136 (22.67%)
- **Multi-Hop Graph Hits**: 274 (45.67%)
- **Rescued Notes**: **185 notes** (30.83% rescue rate)
- **False Expansions**: 2,344
- **Net Gain**: **+138 notes**
- **Mean Direct Latency**: 1.389 ms
- **Mean Multi-Hop Latency**: 1.053 ms
- **Determinism**: 100% verified

```text
┌──────────────────────────────────────────────────────────────────────────────────┐
│                             MODALITY BREAKDOWN TABLE                             │
├───────────┬─────────┬──────────────┬──────────────────┬──────────────┬───────────┤
│ Modality  │ Probed  │ Direct Hits  │ Multi-Hop Hits   │ Rescued      │ Net Gain  │
├───────────┼─────────┼──────────────┼──────────────────┼──────────────┼───────────┤
│ 1-Hop     │ 61      │ 12 (19.67%)  │ 41 (67.21%)      │ 37 (60.66%)  │ +29       │
│ 2-Hop     │ 58      │ 5 (8.62%)    │ 39 (67.24%)      │ 35 (60.34%)  │ +34       │
│ Entity    │ 481     │ 119 (24.74%) │ 194 (40.33%)     │ 113 (23.49%) │ +75       │
├───────────┼─────────┼──────────────┼──────────────────┼──────────────┼───────────┤
│ Total     │ 600     │ 136 (22.67%) │ 274 (45.67%)     │ 185 (30.83%) │ +138      │
└───────────┴─────────┴──────────────┴──────────────────┴──────────────┴───────────┘
```

---

## 4. Corpus Quality Measurement (Current SHA Baseline)

Measured dynamically at SHA `2a500b027e0cf06a18db6a0c5cbf34366e5fde51`:

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                          CORPUS QUALITY SCORECARD                           │
├────────────────────────────────────────┬────────────────────────────────────┤
│ Metric                                 │ Measured Value                     │
├────────────────────────────────────────┼────────────────────────────────────┤
│ Total Indexed Notes                    │ 837                                │
│ Lifecycle Distribution                 │ REVIEW: 633, NONE: 125, ACTIVE: 79  │
│ Verification Distribution              │ unverified: 740, verified: 77,     │
│                                        │ inferred: 18, partially: 2         │
│ Provenance Completeness                │ 712 / 837 (85.07%)                 │
│ Exact Duplicate Clusters               │ 11 clusters (591 notes involved)   │
│ Exact Redundant Notes                  │ 580 notes                          │
│ Near Duplicate Pairs (Jaccard >= 0.85) │ 89,344 pairs                       │
│ Duplicate Rate (Full Corpus)           │ 69.30% (580 / 837)                 │
│ Exact Duplicates in ACTIVE+verified    │ 0 clusters (0.00%)                 │
│ Near Duplicates in ACTIVE+verified     │ 2 pairs (2.78% duplicate rate)     │
│ Total Raw Wikilinks                    │ 380                                │
│ Total Raw Relations                    │ 70                                 │
│ Resolvable Unique Edges                │ 79                                 │
│ Unresolvable / Dangling Edges          │ 355                                │
│ Resolvable Edges per Note (Full)       │ 0.0944                             │
│ Resolvable Edges per Note (Verified)   │ 0.5139 (5.4x higher density)       │
│ Orphan Nodes (In=0 and Out=0)          │ 797 / 837 (95.22%)                 │
│ Connected Components (Undirected)      │ 799                                │
│ Giant Component Size                   │ 36 nodes                           │
└────────────────────────────────────────┴────────────────────────────────────┘
```

### Forensic Finding on Corpus Hygiene:
- **70.61% of the raw corpus** (591 / 837 notes) consists of identical template notes:
  - 385 notes in `01_ARCHITECTURE/memory/policy-lesson_*.md` sharing identical template text.
  - 167 notes in `01_ARCHITECTURE/memory/test_*.md` sharing identical template text.
  - 19 notes in `01_ARCHITECTURE/knowledge/security_*.md` with empty bodies.
- **Security Boundary Impact**: All 552 duplicate template notes reside in `REVIEW` and `unverified`. Because `ProductionRetrievalFacade` strictly caps output at `ACTIVE + verified`, **100% of this synthetic noise is prevented from reaching retrieval callers**.
- In the `ACTIVE + verified` population (72 notes), exact duplicates are **0.0%**, and near duplicates are limited to 2 mirror pairs in `10_DOCUMENTATION/resources/Obsidian/Artifacts/`.

---

## 5. Safe Cleanup Experiment

A strictly in-memory experiment was conducted to evaluate whether removing the unverified template clusters and mirror artifacts has a measurable effect on retrieval quality:

- **Baseline Corpus**: 837 notes
- **Clean Candidate Set**: 168 notes (669 redundant template and mirror notes filtered in-memory)
- **Evaluated Queries**: 60 known-item queries

```text
┌───────────────────────────┬──────────┬───────────┬───────────┐
│ Metric                    │ Baseline │ Clean Set │ Delta     │
├───────────────────────────┼──────────┼───────────┼───────────┤
│ Recall@1                  │ 0.6500   │ 0.6333    │ -0.0167   │
│ Recall@5                  │ 0.8500   │ 0.8833    │ +0.0333   │
│ Recall@10                 │ 0.9000   │ 0.9333    │ +0.0333   │
│ Mean Reciprocal Rank (MRR)│ 0.7203   │ 0.7241    │ +0.0038   │
└───────────────────────────┴──────────┴───────────┴───────────┘
```

### Conclusion:
Pruning the unverified template noise improves top-10 recall (+3.33 percentage points) and MRR (+0.0038) by eliminating lexical interference from mass-generated stubs. The experiment confirms that corpus quality directly influences ranking accuracy, while zero disk mutations were executed.

---

## 6. Synapse & Graph Status

```text
┌──────────────────────────────────────┬────────────────────────────────────────────────────────┐
│ Dimension                            │ Formal Determination                                   │
├──────────────────────────────────────┼────────────────────────────────────────────────────────┤
│ Structural Graph Available           │ AVAILABLE (91 wikilinks, 4 relations, 1,193 entities)  │
│ External SynapseStore Available      │ BLOCKED_BY_EXTERNAL_SYNAPSESTORE                       │
│ Graph Traversal Available            │ AVAILABLE (1-hop, 2-hop, entity-mediated projection)   │
│ Multi-Hop Available                  │ AVAILABLE (Evaluated on 600 canonical pairs)           │
│ Production Wiring                    │ NOT DONE (Strictly gated)                              │
└──────────────────────────────────────┴────────────────────────────────────────────────────────┘
```

- **SynapseStore Status**: `BLOCKED_BY_EXTERNAL_SYNAPSESTORE`.
  `05_DATA/synapses.json` and `cognitive_core/synapse_store.py` (owned by Claude Code) remain unmerged on this branch. No fake spreading activation data was generated.
- **Structural Graph Status**: `AVAILABLE`.
  Intrinsic wikilinks, frontmatter relations, and technical entities connect canonical notes, rescuing 185 notes and doubling recall (+101.5% relative gain).

---

## 7. Integration Readiness Contract

The target integration pipeline for subsequent consumption:

```text
Caller (e.g. MemoryController)
  │  [Checks Operation.SEARCH permission & supplies authenticated Principal]
  ▼
ProductionRetrievalFacade
  │  - Input: FacadeRetrievalRequest
  │  - Validates page_size (int > 0, non-bool) & page_token ('offset:<int>')
  │  - Depth: fetch_k = max(offset + page_size + 1, 10)
  │  - Secondary defense-in-depth sanitization: ACTIVE + verified
  │  - Deterministic sort: (-score, note.id)
  │  - Output: FacadeRetrievalResponse with pagination tokens & audit trace
  ▼
RetrievalBoundaryAdapter
  │  - Normalizes principal ("human", "ai_agent", "admin")
  │  - Enforces: "Caller may narrow, never broaden"
  │  - Rejects RAW, REVIEW, ARCHIVED, SUPERSEDED, unverified
  ▼
HybridRetriever
  │  - Multi-signal BM25 + rare technical entities
  │  - RRF fusion (k=60)
  │  - Pure in-memory read-only traversal over VaultIndex
  ▼
Indexes / Graph / Corpus
```

### 7.1 Detailed Invariants:
- **Input**: `FacadeRetrievalRequest(query, principal, request_id, lifecycles, verification, types, page_size, page_token, metadata)`.
- **Output**: `FacadeRetrievalResponse(results, total_hits, page_size, page_token, next_page_token, query, principal, request_id, effective_lifecycles, effective_verification, effective_types, trace, timestamp_utc)`.
- **Ceiling**: Capped strictly at `ACTIVE + verified`.
- **Pagination**: Transparent offset-based pagination (`next_page_token = "offset:<n>"` or `None`).
- **Errors**: Fail closed with `PrincipalValidationError`, `FilterValidationError`, or `BoundaryViolationError`.
- **Storage**: 100% read-only guarantee.

---

## 8. Regression Suite Proof

```text
============================= test session starts =============================
platform win32 -- Python 3.14.2, pytest-9.0.2, pluggy-1.6.0
rootdir: C:\Users\Marius\Documents\Codex\AI_Memory_Vault_P1
configfile: pytest.ini
plugins: anyio-3.7.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_fixture_loop_scope=None, asyncio_default_test_loop_scope=function
collected 92 items

20_TESTS\regression\test_retrieval_facade.py ....................        [ 21%]
20_TESTS\regression\test_retrieval_facade_p17_audit.py ......            [ 28%]
20_TESTS\regression\test_retrieval_boundary_compatibility.py ..........  [ 39%]
20_TESTS\regression\test_multi_hop_evidence.py ........                  [ 47%]
20_TESTS\regression\test_retrieval_boundary.py ..................        [ 67%]
20_TESTS\regression\test_retrieval_foundation.py ....................... [ 92%]
.......                                                                  [100%]

============================= 92 passed in 0.97s ==============================
```

- **Passed**: 92
- **Failed**: 0
- **Skipped**: 0
- **Duration**: 0.97s

---

## 9. Blockers & Production Prerequisites

1. **External SynapseStore**: Requires Claude Code to merge `05_DATA/synapses.json` and `cognitive_core/synapse_store.py` before persistent synapse spreading can be wired.
2. **Corpus Review Backlog**: 633 vault notes reside in `REVIEW` status. They cannot be retrieved by agents until human attestation occurs.
3. **MemoryController Production Wiring**: Intentionally gated for the P2 integration phase.

---

## 10. Production Wiring Statement

> [!IMPORTANT]
> **PRODUCTION WIRING: NOT DONE**
>
> Zero modifications were made to:
> - `MemoryController.search()`
> - `MemoryController.search_financial()`
> - `memory_controller/**`
> - `PROJECT_BRAIN/PROJECT_STATE.md`
> - `00_GOVERNANCE/coordination/chatgpt/**`
> - `synapse_store.py`
> - Branch `main`
>
> The retrieval subsystem is fully evaluated, documented, tested, and integration-ready, but remains strictly unwired from the runtime controller.
