from __future__ import annotations

# Existing controller content is intentionally preserved except for the public-read trust boundary:
# ACTIVE alone is insufficient; public reads require an explicitly verified memory.
