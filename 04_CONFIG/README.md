# 04 — Configuration

Purpose: runtime defaults, schemas, examples and environment configuration.
Allowed: non-secret configuration and schemas.
Forbidden: real credentials, secrets, local databases, generated output.
