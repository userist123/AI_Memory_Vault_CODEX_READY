/**
 * @alpha
 * Options for loading JSON files.
 */
export interface IJsonFileOptions {
  /**
   * If true, the file is optional and will not throw an error if it does not exist.
   * @default false
   */
  optional?: boolean;
}
