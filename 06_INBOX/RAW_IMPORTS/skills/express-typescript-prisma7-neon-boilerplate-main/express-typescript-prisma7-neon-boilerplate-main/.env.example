# Neon PostgreSQL connection string (free tier works perfectly)
DATABASE_URL=

# Secret key for JWT signing
# Generate a strong one using this command:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Server port
PORT=5000