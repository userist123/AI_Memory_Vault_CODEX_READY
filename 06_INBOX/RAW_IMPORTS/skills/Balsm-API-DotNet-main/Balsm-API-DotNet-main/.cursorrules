Read and follow the agent instructions in ../Balsam-Roadmap/agents/rules/AGENTS.md.

API-specific: .NET modular monolith. Respect module and layer boundaries. All endpoints require auth and permission checks. Use parameterized queries. Soft-delete only. Every new endpoint needs integration tests.
